/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // React Strict Mode is off,
  images: {
    domains: ["codehelp-apis.vercel.app"],
  },
};

module.exports = nextConfig;
